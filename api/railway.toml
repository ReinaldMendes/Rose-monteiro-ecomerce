[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run prisma:generate && npx prisma migrate deploy && npx prisma db seed && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3